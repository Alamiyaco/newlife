const chartStore={};function destroyChart(id){if(chartStore[id]){chartStore[id].destroy();delete chartStore[id]}}function makeChart(id,type,labels,values,options={}){destroyChart(id);const ctx=document.getElementById(id);if(!ctx)return;chartStore[id]=new Chart(ctx,{type,data:{labels,datasets:[{label:options.label||"القيمة",data:values,borderWidth:2.5,tension:.38,fill:options.fill??false,pointRadius:4,pointHoverRadius:7}]},options:{responsive:true,maintainAspectRatio:false,plugins:{legend:{display:options.legend??type==="doughnut"},tooltip:{callbacks:{label(context){const val=context.raw??0;return options.money?money(val):String(val)}}}},scales:type==="doughnut"?{}:{y:{beginAtZero:true,grid:{color:"rgba(14, 49, 64, .08)"},ticks:{callback(value){return options.money?money(value):value}}},x:{grid:{display:false}}}}})}
